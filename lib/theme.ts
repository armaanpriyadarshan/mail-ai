// Single source of truth for colors / spacing referenced from JS (Animated, etc).
// Tailwind reads its own copy from tailwind.config.js — keep them in sync.
export const colors = {
  paper: "#FBF7F1",
  ink: "#1F1B16",
  muted: "#7A6F62",
  line: "#EAE2D3",
  card: "#FFFFFF",
  accent: "#E26A2C",
  accentSoft: "#FBE3D1",
  success: "#3C8C5C",
  danger: "#C8553D",
};

export const radii = { card: 12, pill: 999 };
export const spacing = { card: 16 };
